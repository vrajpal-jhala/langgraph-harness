- [x] langgraph-harness Thread link in status note
- [-] Acceptance-signal tracking - the gate metric, from live traffic. Two stages:
  - [x] Reviewer-side thread analytics - FE-only: `getThreadMetrics(runs)` counts opened/resolved from the agent's own tool_input events (create_draft_note+position / resolve_merge_request_thread) across all runs, shown under the run list. No backend — the FE already has every run's events (same source as the todo list). Reviewer-side only: "resolved" = langgraph-harness resolved, not developer acceptance.
  - [ ] Developer-truth acceptance (fixed / ignored / dismissed) - classify each thread's fate from GitLab state at merge/close, where no re-review runs. LangGraph workflow vs plain webhook handler decided when built (workflow needs generalizing the single-graph layer into a registry). This is the actual gate metric.
- [ ] FS + git read surface (staged) - read-only Filesystem MCP + intent git tool against the existing checkout first; worktrees only when same-repo concurrency collides. Biggest accuracy unlock.
- [ ] MR description generator - tools wired, high visible value, near-zero trust risk.
- [-] Context meter increment - section breakdown + delta events on the existing counter; exact tokenization stays deferred.
  - [x] Actual usage from Ollama/OpenRouter responses - `usage_metadata` (prompt/completion/total tokens) already normalized by LangChain's ChatOllama/ChatOpenRouter, emitted as a `context_usage` run event alongside the existing message event, no new transport. FE shows running total + %-of-contextWindow next to each run's duration, and a delta line per turn in the timeline.
  - [ ] Section breakdown (System/Conversation/Repository/Tools/etc.) - needs either the HF-tokenizer live estimator or per-source char-length instrumentation at prompt-assembly time; Ollama only returns two aggregate numbers per call, not a per-section split.
  - [x] Live pre-call estimation via HF tokenizer.json - deferred per design doc; actual post-response counts are the only source for now.
- [x] Context-window overflow prevention - `summarizationMiddleware` clears old messages into a summary once a run nears `contextWindow`, sized via `AutoTokenizer.from_pretrained`'s tokenizer.json estimate (with a safety-margin buffer for its known undercount on tool-heavy turns) instead of letting Ollama silently truncate. Backend emits a `summarization` run event; FE surfacing still pending.
- [-] Tool-usage detectors (shadow) - Repeated Tool + Redundant Observation detectors fed by the existing event stream: hash (tool, normalized args) for consecutive-repeat/loop detection, hash normalized output for duplicate context pulls. Flush into RunSummary (repeatedToolCalls) + flag the run for the timeline/needs-attention. Shadow-only, hard-coded thresholds; guardrail middleware (mid-run reminders) waits for would-fire data.
- [ ] Supermemory context records + recall - ingest one context_record per review directly into supermemory, surface overlapping history to the reviewer. No interim layer.
- [x] Per-repo custom review rules - a review-rules.md per repo (team conventions, forbidden patterns, "always check X in this service") loaded into the reviewer's context. The skill infra already loads .md files, so this is nearly free - and it's the lite version of the load_custom_rules item already sitting in the implementation plan. High value because team-specific findings are the ones generic reviewers can't make.
- [ ] Linked-issue context injection - pull the linked issue, its description, and acceptance criteria (issue tools already wired) into the review prompt. The lite version of Phase-2's User Story Verification: the reviewer stops judging code in a vacuum and can flag "this doesn't do what the issue asked" - a comment class humans value highly.
- [x] Critic pass - before publishing, one extra LLM call reviewing the drafted comments themselves: "which of these would a senior dev roll their eyes at?" Drops weak findings. With a local 35B model this is probably the cheapest precision win available - one call per review, no new infra.
- [ ] Inline-position validation + fallback - finish the "position checks - partial" guard: verify each inline comment's diff position is valid before publishing; fall back to a file-level comment if not. Misplaced inline comments look broken in a way wrong opinions don't.
- [ ] Repo human-review memories - build/update a per-repo memory from past review threads and runs on the same repo: distill recurring findings, patterns the human accepted/dismissed, and repo-specific review style into a memory that surfaces to the reviewer at runtime.
- [ ] Golden-set replay - parked until live metrics exist and the product matures: curate ~20-50 past MRs with known ground truth on the existing benchmark harness; re-run + score on every prompt/skill/model change.
- [x] Update tour guide for new features

Folded into the code-review skill prompt (no separate items): severity/confidence filter (block-worthy comments only, no nitpicks/praise).
