# Demo Todo App

A tiny todo API — not a real product, just a fixture that `langgraph-harness` reviews and resolves issues against for demo screenshots.

## Run

```bash
npm install
npm start   # listens on :4000
```

## Test

```bash
npm test
```
